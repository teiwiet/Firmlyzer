#include<iostream>

using namespace std;


void detect_credentials(const string& path);
void detect_private_keys(const string& path);
void detect_dangerous_functions(const string& path);
void detect_command_injection(const string& path);

